# CareFlow live E2E verification — 2026-09-04

합성·비식별 정상 상담 시나리오를 Codespaces의 실제 UI 경로에서 실행해 Claude 생성부터 데이터 수명주기까지 확인했다. 임상 검증이나 실제 환자 성능을 의미하지 않는다.

## 실행 경로

`WebSocket text → Claude Sonnet 5 structured S/O/P → evidence gate → READY → transcript purge`

## 확인 결과

- session: `#da0aedb1`
- final status: `READY`
- finalize latency shown in UI: `6770 ms`
- Subjective evidence: `#1`, `#2`
- Objective evidence: `#3`
- Plan evidence: `#4`
- Assessment: schema/Pydantic boundary에서 차단
- DB: `healthy`
- Transcript store: `healthy`
- Purge: `purged`
- audit timeline에서 `session.finalized`와 `transcript.purged` 확인
- 입력 상태: 사용 가능, 음성 인식 활성

이 검증은 이전에 정상 합성 세션이 `REVIEW_REQUIRED`로 떨어지던 live generation 실패 이후 재실행한 것이다. 별도 `make live-claude` 호출에서도 같은 날 Claude Sonnet 5 structured S/O/P 계약이 성공했고, 단일 합성 케이스 latency `5217.679 ms`를 기록했다.

## 해석 경계

- 현재 결과는 합성 정상 시나리오 1건의 live E2E 성공 증거다.
- 실제 마이크 입력, 억양, 배경 소음 환경의 최종 검증은 별도 과제로 남는다.
- `READY`와 `purged`는 제품 수명주기 계약이 정상 동작했음을 의미하며 임상적 정확도나 안전성을 보증하지 않는다.
